<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= post.title %> | PROPOSITION T</title>
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;600&family=Noto+Sans+KR:wght@300;500;700&display=swap" rel="stylesheet">
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { 
            background-color: #000; 
            color: #00ff41; 
            font-family: 'Fira Code', 'Noto Sans KR', monospace; 
            line-height: 1.6;
            padding: 20px;
        }
        .container { 
            width: 100%; 
            max-width: 850px; 
            margin: 20px auto; 
            background: rgba(0, 20, 0, 0.95); 
            border: 1px solid #00ff41; 
            padding: 30px; 
            box-shadow: 0 0 30px rgba(0, 255, 65, 0.1); 
        }
        
        /* 제목 및 메타 정보 정렬 */
        h1 { font-size: 1.6rem; color: #00ff41; margin-bottom: 20px; text-transform: uppercase; border-left: 4px solid #00ff41; padding-left: 15px; }
        .meta-data { font-size: 0.85rem; color: rgba(0, 255, 65, 0.75); display: flex; flex-wrap: wrap; gap: 20px; margin-bottom: 30px; border-bottom: 1px solid rgba(0, 255, 65, 0.3); padding-bottom: 15px; }
        .meta-item { display: flex; gap: 8px; }
        .label { color: #00ff41; font-weight: 600; }

        /* 본문 및 기계어 코드 박스 */
        .content { color: #e0ffe6; font-size: 0.95rem; }
        pre { 
            background: #001100; 
            border: 1px solid #00ff41; 
            padding: 20px; 
            margin: 25px 0; 
            overflow-x: auto; /* 가로 스크롤 허용 */
            white-space: pre; /* 줄바꿈 유지 */
            font-size: 0.9rem;
            color: #00ff41;
            box-shadow: inset 0 0 10px rgba(0, 255, 65, 0.2);
        }
        code { font-family: 'Fira Code', monospace; }

        .btn-back { display: inline-block; margin-top: 30px; padding: 10px 20px; background: #00ff41; color: #000; text-decoration: none; font-weight: 700; font-size: 0.85rem; }
        .btn-back:hover { background: #fff; box-shadow: 0 0 20px #fff; }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= post.title %></h1>
        <div class="meta-data">
            <div class="meta-item"><span class="label">[DATE]</span> <%= post.date %></div>
            <div class="meta-item"><span class="label">[FROM]</span> <%= post.sender %></div>
            <div class="meta-item"><span class="label">[TO]</span> <%= post.receiver %></div>
        </div>
        <div class="content">
            <%- post.content %>
        </div>
        <a href="/" class="btn-back">< RETURN_TO_SYSTEM</a>
    </div>
</body>
</html>