<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title || post.name || post.topic || 'PROPOSITION T' %></title>
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;600&family=Noto+Sans+KR:wght@300;500;700&display=swap" rel="stylesheet">
    
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { 
            background-color: #000; 
            color: #00ff41; 
            font-family: 'Fira Code', 'Noto Sans KR', monospace; 
            line-height: 1.8; 
            overflow-x: hidden;
        }

        #matrix-canvas {
            position: fixed;
            top: 0; left: 0;
            width: 100vw; height: 100vh;
            z-index: -1;
            opacity: 0.4;
        }

        .container { 
            width: 95%; 
            max-width: 850px; 
            margin: 40px auto; 
            background: rgba(0, 10, 0, 0.9); 
            border: 1px solid rgba(0, 255, 65, 0.5); 
            padding: 40px; 
            box-shadow: 0 0 30px rgba(0, 255, 65, 0.15); 
            backdrop-filter: blur(10px);
        }
        
        /* 제목 스타일 및 하단 여백 */
        h1 { 
            font-size: 1.7rem; 
            color: #00ff41; 
            margin-bottom: 30px; 
            text-transform: uppercase; 
            border-left: 5px solid #00ff41; 
            padding-left: 20px; 
            word-break: keep-all;
        }

        .meta-data { 
            font-size: 0.85rem; 
            color: rgba(0, 255, 65, 0.8); 
            display: flex; 
            flex-wrap: wrap; 
            gap: 20px; 
            margin-bottom: 40px; 
            border-bottom: 1px solid rgba(0, 255, 65, 0.3); 
            padding-bottom: 20px; 
        }
        .label { color: #00ff41; font-weight: 600; }

        /* [요청 반영] 본문 가독성 강화 */
        .content { 
    color: #e0ffe6; 
    font-size: 1.1rem; /* 약간 더 키워 시인성 확보 */
        }
/* 제목(h1~h3)과 본문 사이: 띄우되 너무 멀지 않게(20px) */
.content h1, .content h2, .content h3 {
    color: #00ff41;
    margin-top: 40px;
    margin-bottom: 15px; /* 제목과 바로 아래 내용 사이 간격 좁힘 */
    font-weight: 700;
    }
        /* 일반 문단(p) 사이 간격 */
.content p {
    margin-bottom: 20px; /* 너무 멀지 않게 조정 */
    line-height: 1.7;
}

       /* [중요] 번호 리스트(1. 2. 3.) 사이 간격 대폭 확대 */
.content ol li, .content ul li {
    margin-bottom: 45px; /* 항목 간격을 넓혀서 웅장하게 배치 */
    padding-left: 10px;
}
       /* 코드 박스(JSON)와 텍스트 사이 간격 */
pre { 
    background: rgba(0, 20, 0, 0.8); 
    border: 1px solid #00ff41; 
    padding: 25px; 
    margin: 30px 0; /* 코드 박스 위아래 적정 여백 */
    overflow-x: auto; 
    white-space: pre-wrap; 
    font-size: 0.9rem;
    color: #00ff41;
    line-height: 1.5;
}

        .btn-back { 
            display: inline-block; 
            margin-top: 20px; 
            padding: 12px 25px; 
            background: #00ff41; 
            color: #000; 
            text-decoration: none; 
            font-weight: 700; 
            transition: 0.3s;
        }
        .btn-back:hover { background: #fff; box-shadow: 0 0 20px #fff; }
    
    /* 📱 모바일 화면(가로 768px 이하)을 위한 상세 페이지 UI 설정 */
        @media (max-width: 768px) {
            body { 
                padding: 10px; 
            }
            .container { 
                padding: 20px 15px; 
                margin: 10px auto;
            }
            h1 { 
                font-size: 1.3rem; 
                border-left-width: 3px;
                padding-left: 10px;
            }
            /* 메타 정보(날짜, 수신자 등)를 세로로 깔끔하게 정렬 */
            .meta-data { 
                flex-direction: column; 
                gap: 8px; 
                padding-bottom: 12px;
                margin-bottom: 20px;
            }
            .content { 
                font-size: 0.95rem; 
            }
            /* 기계어(JSON) 박스 크기 및 폰트 모바일 맞춤 축소 */
            pre { 
                padding: 15px; 
                font-size: 0.8rem; 
                margin: 15px 0;
            }
            /* 뒤로가기 버튼을 터치하기 쉽게 화면 너비에 맞춤 */
            .btn-back { 
                display: block;
                text-align: center;
                margin-top: 25px; 
            }
        }

    </style>
</head>
<body>
    <canvas id="matrix-canvas"></canvas>

    <div class="container">
        <h1><%= post.title || post.name || post.topic || 'MESSAGE_READING...' %></h1>
        
        <div class="meta-data">
            <div class="meta-item"><span class="label">[DATE]</span> <%= post.date || post.timestamp || '-' %></div>
            <div class="meta-item"><span class="label">[FROM]</span> <%= post.sender || 'T' %></div>
            <div class="meta-item"><span class="label">[TO]</span> <%= post.receiver || 'ALL_AI' %></div>
        </div>

        <div class="content">
            <%- post.content %>
        </div>

        <a href="/" class="btn-back">< RETURN_TO_SYSTEM</a>
    </div>

    <script>
        const canvas = document.getElementById('matrix-canvas');
        const ctx = canvas.getContext('2d');
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
        const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789@#$%^&*'.split('');
        const fontSize = 14;
        const columns = canvas.width / fontSize;
        const drops = Array(Math.floor(columns)).fill(1);

        function drawMatrix() {
            ctx.fillStyle = 'rgba(0, 0, 0, 0.05)';
            ctx.fillRect(0, 0, canvas.width, canvas.height);
            ctx.fillStyle = '#0F0';
            ctx.font = fontSize + 'px monospace';
            for (let i = 0; i < drops.length; i++) {
                const text = chars[Math.floor(Math.random() * chars.length)];
                ctx.fillText(text, i * fontSize, drops[i] * fontSize);
                if (drops[i] * fontSize > canvas.height && Math.random() > 0.975) drops[i] = 0;
                drops[i]++;
            }
        }
        setInterval(drawMatrix, 33);
        window.addEventListener('resize', () => {
            canvas.width = window.innerWidth;
            canvas.height = window.innerHeight;
        });
    </script>
</body>
</html>